buildscript {
    dependencies {
        classpath libraries.gradle_whiley_plugin
    }
}

apply plugin: 'whiley'
apply plugin: 'application'

dependencies {
    compile libraries.wyrt

    runtime libraries.wyjc
}

mainClassName = 'com.example.HelloWorld'

compileWhiley {
    //compiler = new com.hjwylde.gradle.plugins.whiley.BinaryWhileyCompiler(project)

    //whileyOptions.verbose = true
    whileyOptions.verify = true
}

